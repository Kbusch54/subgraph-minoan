{
  "name": "minoan-exchange",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ minoan-exchange",
    "create-local": "graph create --node http://localhost:8020/ minoan-exchange",
    "remove-local": "graph remove --node http://localhost:8020/ minoan-exchange",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 minoan-exchange",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.49.0",
    "@graphprotocol/graph-ts": "0.29.1",
    "ethers": "^6.4.1",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
